/* Terminal colors (16 first used in escape sequence) */
static const char *colorname[] = {
	/* 8 normal colors */
	"{{lower primary.black.hex }}",
	"{{lower primary.red.hex }}",
	"{{lower primary.green.hex }}",
	"{{lower primary.yellow.hex }}",
	"{{lower primary.blue.hex }}",
	"{{lower primary.magenta.hex }}",
	"{{lower primary.cyan.hex }}",
	"{{lower primary.white.hex }}",

	/* 8 bright colors */
	"{{lower primary.bright_black.hex }}",
	"{{lower primary.bright_red.hex }}",
	"{{lower primary.bright_green.hex }}",
	"{{lower primary.bright_yellow.hex }}",
	"{{lower primary.bright_blue.hex }}",
	"{{lower primary.bright_magenta.hex }}",
	"{{lower primary.bright_cyan.hex }}",
	"{{lower primary.bright_white.hex }}",

	[255] = 0,

	/* more colors can be added after 255 to use with DefaultXX */
	"{{lower primary.bright_white.hex }}", /* foreground */
	"{{lower primary.black.hex }}", /* background */
	"{{lower primary.yellow.hex }}", /* cursor */
};

/*
 * Default colors (colorname index)
 * foreground, background, cursor, reverse cursor
 */
unsigned int defaultfg = 256;
unsigned int defaultbg = 257;
unsigned int defaultcs = 258;
static unsigned int defaultrcs = 257;
