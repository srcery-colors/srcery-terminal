#!/bin/bash

# Save this script into srcery_guake.sh, make this file executable and run it:
#
# $ chmod +x srcery_guake.sh
# $ ./srcery_guake.sh
#
# Alternatively copy lines below directly into your shell.

gconftool-2 -s -t string /apps/guake/style/background/color '{{double black.hex }}'
gconftool-2 -s -t string /apps/guake/style/font/color '{{double bright_white.hex }}'
gconftool-2 -s -t string /apps/guake/style/font/palette '{{double black.hex }}:{{double red.hex }}:{{double green.hex }}:{{double yellow.hex }}:{{double blue.hex }}:{{double magenta.hex }}:{{double cyan.hex }}:{{double white.hex }}:{{double bright_black.hex }}:{{double bright_red.hex }}:{{double bright_green.hex }}:{{double bright_yellow.hex }}:{{double bright_blue.hex }}:{{double bright_magenta.hex }}:{{double bright_cyan.hex }}:{{double bright_white.hex }}'
